# Mongo-Postgres-SQL-data-replication
This is a practice project that helps in setting up the mongo db from the data of posgtgres db and vice-versa..

You will need to install 2 modules :
```
pip install pymongo psycopg2-binary
````
